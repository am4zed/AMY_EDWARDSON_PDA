### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby
### Testing task 2 code:

# Carry out dynamic testing on the code below.
# Correct the errors below that you spotted in task 1.

require_relative('card.rb') # filepath should be './card.rb'
class CardGame


  def checkforAce(card) #should be 'check_for_ace'
    if card.value = 1
      return true
    else
      return false
    end
  end

  dif highest_card(card1 card2)
  # should be 'def highest_card(card1, card2)
  if card1.value > card2.value
    return card.name # Card class has no name parameter, should be 'card1'
  else
    card2
  end
end
end # extra end, ends class. Improper indentation

def self.cards_total(cards) # method outside of class
  total # should be total = 0
  for card in cards
    total += card.value
    return "You have a total of" + total
  end
end


```
